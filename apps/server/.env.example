# Copy to .env and fill in values. Loaded by your shell / process manager (Nest does not read .env automatically).

# --- Core ---
DATABASE_URL=postgresql://vigil:vigil@localhost:5432/vigil?schema=public

# HTTP listen port (optional, default 3000)
# PORT=3000

# --- Watch alert email (optional; both SMTP_HOST and MAIL_FROM required to enable) ---
# See docs/email-alerts.md for behavior, Mailpit local setup, and deliverability notes.

SMTP_HOST=
SMTP_PORT=587
# true = implicit TLS (e.g. port 465); false = STARTTLS on 587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

MAIL_FROM=
# MAIL_REPLY_TO=

# Canonical public origin of the Vigil SPA (watch links in alert bodies)
PUBLIC_BASE_URL=http://localhost:5173
